# Release claim

The v0.1.0 release may claim only that:

- the package implements the documented deterministic calculation protocol;
- synthetic public fixtures validate the end-to-end workflow;
- 523 tests exercise core, matrix, solver and integrity behavior;
- committed runs regenerate under the supported Python versions;
- the verifier detects the declared nine tamper classes;
- release assets are content-addressed.

It may not claim that any current real-world export price, tariff, tax, Incoterms allocation, buyer payment, product quality or profit outcome has been validated.
