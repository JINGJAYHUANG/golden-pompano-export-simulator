# Repository governance

The repository favors reviewed, linear history and reproducible evidence. Calculation changes should be proposed through pull requests, pass the complete CI matrix and preserve the public-data boundary. Semantic-version tags are immutable, and release assets are rebuilt from the tagged source.
